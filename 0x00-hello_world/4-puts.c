#include <stdio.h>
/*
 *main - entry point
 *
 *Desciption: Prints a string using puts.
 *The string includes the an open double-quotes mark.
 *Used the escape character \ to ensure quote is displayed.
 *Return: returns an int 0
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
